[project]
name = "BiCP"
version = "1.0.0"
description = "A lightweight desktop IDE designed specifically for Competitive Programming."
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.7",
]

[project.optional-dependencies]
build = [
    "pyinstaller>=6.0",
]
